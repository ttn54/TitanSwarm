# ─────────────────────────────────────────────────────────────────────────────
# TitanSwarm — Development dependencies
#
# For production Docker builds, use requirements-prod.txt instead (pinned).
# Install:  pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# --- Web framework & UI ---
streamlit>=1.55.0
watchdog>=6.0.0

# --- Data & validation ---
pydantic>=2.12.0
pydantic-settings>=2.9.0
pandas>=2.2.0
numpy>=1.26.0

# --- Database ---
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.21.0
asyncpg>=0.30.0

# --- AI / ML (CPU) ---
torch>=2.0.0  --index-url https://download.pytorch.org/whl/cpu
sentence-transformers>=5.0.0
faiss-cpu>=1.13.0
scikit-learn>=1.5.0

# --- LLM API clients ---
openai>=2.0.0
google-genai>=1.0.0

# --- Web scraping ---
python-jobspy>=1.1.0
playwright>=1.50.0
beautifulsoup4>=4.14.0
lxml>=6.0.0
requests>=2.32.0
httpx>=0.28.0

# --- PDF generation ---
Jinja2>=3.1.0
pillow>=12.0.0
pdfplumber>=0.11.0

# --- Auth & security ---
bcrypt>=5.0.0

# --- Utilities ---
GitPython>=3.1.0
PyYAML>=6.0.0
tenacity>=9.0.0
python-dateutil>=2.9.0
typer>=0.24.0

# --- Testing (dev only) ---
pytest>=9.0.0
pytest-asyncio>=1.0.0
pytest-httpx>=0.36.0
pytest-playwright>=0.7.0
